const c=document.getElementById('game'),x=c.getContext('2d'),n=20,s=30;let snake,dir,food,score,timer,running;
function start(){snake=[{x:10,y:10},{x:9,y:10},{x:8,y:10}];dir={x:1,y:0};score=0;running=true;place();clearInterval(timer);timer=setInterval(tick,120);msg.textContent='Hunt the cockroaches!'}
function place(){do{food={x:Math.floor(Math.random()*n),y:Math.floor(Math.random()*n)}}while(snake.some(p=>p.x===food.x&&p.y===food.y))}
addEventListener('keydown',e=>{let k=e.key.toLowerCase(),d=k==='arrowup'||k==='w'?{x:0,y:-1}:k==='arrowdown'||k==='s'?{x:0,y:1}:k==='arrowleft'||k==='a'?{x:-1,y:0}:k==='arrowright'||k==='d'?{x:1,y:0}:null;if(d&&!(d.x===-dir.x&&d.y===-dir.y))dir=d});
function tick(){let h={x:snake[0].x+dir.x,y:snake[0].y+dir.y};if(h.x<0||h.y<0||h.x>=n||h.y>=n||snake.some(p=>p.x===h.x&&p.y===h.y)){running=false;clearInterval(timer);msg.textContent='GAME OVER — Cockroaches crushed: '+score;return}snake.unshift(h);if(h.x===food.x&&h.y===food.y){score++;place();msg.textContent='💥 CRUSHED!'}else snake.pop();document.getElementById('score').textContent=score;draw()}
function draw(){x.fillStyle='#090909';x.fillRect(0,0,600,600);x.strokeStyle='#202020';for(let i=0;i<=n;i++){x.beginPath();x.moveTo(i*s,0);x.lineTo(i*s,600);x.stroke();x.beginPath();x.moveTo(0,i*s);x.lineTo(600,i*s);x.stroke()}x.font='25px serif';x.fillText('🪳',food.x*s+2,food.y*s+27);snake.forEach((p,i)=>{x.fillStyle=i?'#7fbf3f':'#d5ef62';x.fillRect(p.x*s+3,p.y*s+3,s-6,s-6)})}draw();